单次访问页面产生多条服务端请求的原因:
用户手动仅触发HTML首页1次请求；浏览器解析页面后按需发起图片资源请求，叠加浏览器默认图标请求，最终服务端收到多条独立HTTP访问日志。




浏览器加速HTML页面渲染相关技术：
一、HTTP层优化
浏览器对同一个域名开启多条TCP连接（通常6～8个），并发下载页面内图片、图标等多个静态资源，串行改并行，缩短整体资源加载耗时。
头部`Connection: keep-alive`复用一条TCP链路连续收发多次请求，省去频繁建立、断开TCP三次握手/四次挥手开销，减少连接耗时。
服务端对HTML、CSS、JS等文本资源压缩后传输，浏览器接收后解压，降低网络传输字节量。

二、浏览器缓存机制
资源在有效期内直接从本地磁盘/内存读取，不再向web服务器发送HTTP请求，省去网络往返。
缓存过期后浏览器携带标识询问服务器资源是否修改，未修改则返回304，不重复下发完整文件。

三、页面加载与渲染优化
HTML解析阶段提前对页面外链域名做DNS解析，后续请求资源时省去域名解析耗时。
提前加载页面即将用到的关键资源，等DOM渲染需要时直接取用。
浏览器采用解析HTML生成DOM树→解析CSS生成CSSOM→合成渲染树→布局→绘制流水线，边下载HTML边分段解析渲染（流式解析），无需等待全量HTML下载完毕再展示页面。
非首屏图片延迟请求，优先完成首屏页面渲染，减少首屏资源请求数量。

四、传输协议升级
HTTP/2、HTTP/3支持多路复用，单条TCP连接并行承载多个资源请求，消除HTTP1.1同域连接数限制，大幅提升批量静态文件下载效率。
