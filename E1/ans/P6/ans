 日志耗时总结
 1. 业务处理极快
`read_request/open_file/send_header/send_file_data` 单请求业务耗时大多零点几ms~百ms内，文件读取、报文收发本身无性能瓶颈，服务器软硬件资源充足。

 2. 瓶颈唯一在`accept_wait`（连接等待耗时）
1. 第一个请求`accept_wait=10151.612ms(10s)`、后续多轮图片请求后的html请求等待分别977.245ms、743.504ms；图片资源请求`accept_wait`仅十几微秒。
2. 成因：单线程串行模型+末尾sleep(1)+响应头固定Connection:close短连接
    - 一轮页面刷新产生`index.html+nigel.jpg`2次请求，处理完1个请求sleep(1)阻塞主线程，新连接在内核TCP全连接队列积压；
    - 主线程处理完上一个请求才回到`accept`，等待积压连接导致accept耗时暴增。

 3. 现象对应业务
浏览器频繁刷新，每次刷新产生2条GET请求，请求排队累加等待，直观表现页面长时间空白延迟加载。

 4. 结论
页面加载慢不是IO/CPU/文件处理慢，根源：单线程串行+强制sleep(1)+HTTP短连接，大量连接排队阻塞accept。
