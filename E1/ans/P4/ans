频繁刷新页面延迟展示原因分析：
日志所有返回头都是`Connection: close`，浏览器每加载一个资源就新建TCP三次握手→传输→四次挥手断开。
连续快速刷新瞬间批量创建海量短连接，内核TCP协议栈端口、连接表耗尽，新连接排队等待，浏览器阻塞等待握手，页面迟迟不渲染。

一个HTML页面依赖`nigel.jpg`图片，必须等index.html返回后，浏览器才发起图片请求。
服务端同一时间只能处理有限连接，密集刷新下请求在服务端请求队列排队，后续请求被积压，页面加载延后。

单线程/少量工作线程处理请求，无异步IO、连接复用。短时间爆发大量GET请求，服务端处理队列溢出，新请求得不到调度，出现长时间等待。

浏览器同源并行请求数有限，疯狂刷新瞬间堆积大量待发请求，浏览器请求池占满，新请求排队休眠，间隔很久才轮询发送请求，表现为隔很久才出页面。
