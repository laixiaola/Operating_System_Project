原因分析
日志成对重复出现：`request(index)→SEND(index)→Header 200`、`request(nigel.jpg)→SEND→Header 200` 不断循环计数上涨，每一次刷新浏览器都新建一条客户端连接。

频繁刷新产生大量排队TCP连接
快速点击刷新，浏览器瞬间批量发起大量GET请求（index.html+图片nigel.jpg），内核TCP全连接队列塞满，多余连接进入等待队列阻塞排队。
单线程串行逐个处理，后面请求排队等待
服务处理完1组（html+图片）才取下一个连接，请求量远超处理速度，连接不断积压，新的刷新请求排在队列末尾，需要等前面几十条连接全部处理完毕才被调度。

日志`Connection: close`，每次资源发送完成立刻断开套接字，不能长连接复用，每次刷新都要三次握手新建连接，新建连接耗时+排队耗时叠加，页面延迟很久才渲染。
页面由html+图片两个资源组成，一次浏览器刷新实际产生2次HTTP请求，刷新N次就产生2*N个任务，任务量翻倍，排队更严重。

总结
nweb单线程串行处理+短连接close+频繁刷新大批量并发请求，连接排队积压，新请求长时间得不到服务，浏览器等待报文接收，因此隔很久才显示页面。
